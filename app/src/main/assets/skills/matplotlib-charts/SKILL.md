---
name: matplotlib-charts
description: Create charts from data using chart_generate or python_execute with matplotlib patterns.
version: 1.0.0
---

# Matplotlib Charts

Use `chart_generate(title, labels, values)` or python_execute:
`labels = ['A','B']; values = [1,2,3]; plt.savefig` → chart saved as PNG.