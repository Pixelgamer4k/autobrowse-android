---
name: pdf-generation
description: Generate PDF documents from research, page content, or agent summaries using pdf_generate or python_execute.
version: 1.0.0
---

# PDF Generation

Use `pdf_generate(title, content)` or `python_execute` with reportlab/fpdf patterns.
Capture page text via `browser_snapshot` first, then format into sections.