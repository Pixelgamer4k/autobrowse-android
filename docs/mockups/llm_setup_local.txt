The image shows a smartphone screen displaying a dark-themed mobile app interface titled **"LLM Setup"**. The phone has a thin bezel, a centered front camera notch at the top, and a black background with white/gray text and purple accents.

### Top Section
- A back arrow and the title **"LLM Setup"**.
- Below the title, a small dark gray box contains two lines of text:
  - `t-mmpproj-F16.g`
  - `guf`

### Main Model Card
A prominent dark rectangular card occupies the center, describing a model called **"Gemma 4 E4B"**:
- Subtitle: **"Stronger"**
- Description lines:
  - `Gemma edge ·`
  - `vision + audio`
  - `+ tools · 128K`
  - `ctx`
- Size listed as: **"~6.5 GB (Q4 + vision)"**
- Two long file paths (partially wrapped):
  - `gemma-4-E4B-i`
  - `t-Q4_K_M.gguf +`
  - `gemma-4-E4B-i`
  - `t-mmpproj-F16.g`
  - `guf`
- An empty circular radio button appears on the left side of the card.
- On the right side of the card:
  - A download arrow icon with the text **"Download to phone"**
  - The word **"HuggingFace"** in smaller text beneath it.

### Inference Backend Section
- Header text: **"Inference backend"**
- Two toggle buttons:
  - **"CPU"** (selected, filled with purple)
  - **"GPU"** (unselected, dark outline)
- Subtext: **"CPU: widest compatibility, slowest."**

### Additional Controls
- A rounded rectangular button with a folder icon labeled **"Import .gguf file"**
- Status message: **"No model downloaded yet."**
- At the bottom, two buttons side by side:
  - **"Test"** (with a circular refresh icon)
  - **"Save"** (grayed out, less prominent)

The overall UI style is minimalist and modern, using a dark color palette (black, dark gray, white, and purple highlights). The interface appears designed for configuring and downloading on-device large language models, specifically variants of a "Gemma 4" model with multimodal capabilities.
